<IfModule mod_rewrite.c>
    RewriteEngine On
    
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    
    # Sinon, on redirige absolument tout vers notre fichier index.php
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>